@using Vfront.Extensions
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title - My ASP.NET MVC Application</title>
    <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='http://fonts.googleapis.com/css?family=Pathway+Gothic+One' rel='stylesheet' type='text/css'>
    @Styles.Render("~/Content/basicCss")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div id="wrap">
        <!-- Begin page content -->
        <header>
            <div class="contentwrapper clearfix">
                <div>
                    <h1 id="sitetitle">@Html.ActionLink("Bootstrap Demo", "Index", "Home")</h1>
                </div>
                <div>
                    <nav>
                        <ul class="nav nav-pills">
                            <li class="@Html.GetCurrentTabCssClass("Home")">@Html.ActionLink("Home", "Index", "Home")</li>
                            <li class="@Html.GetCurrentTabCssClass("Knowledge")">@Html.ActionLink("Knowledge Sharing", "Index", "Knowledge")</li>
                            <li class="@Html.GetCurrentTabCssClass("Ef")">@Html.ActionLink("ITIS Team", "Index", "Ef")</li>
                            <li class="@Html.GetCurrentTabCssClass("Elastislide")">@Html.ActionLink("Elastislide", "Index", "Elastislide")</li>
                        </ul>
                    </nav>
                </div>
            </div>
        </header>
        <div id="body">
            @RenderBody()
        </div>

        <div id="push"></div>
    </div>

    <footer>
        <div class="container">
            <p class="muted credit">Powered by Bootstrap | @Html.ActionLink("Developing Log", "About", "Home")</p>
        </div>
    </footer>

    @Scripts.Render("~/bundles/basic")
    @RenderSection("loadResources", required: false)
    @RenderSection("scripts", required: false)
</body>
</html>
